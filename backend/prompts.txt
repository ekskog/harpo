I want to create a simple web app where i can showcase my writing. 
I aggregate written pieces into collections. 
A collection will have one or more written pieces and an image to illustrate it.
Every written piece has a timestamp (MM/YYYY) and an image to illustrate it. 

I want a simple landing page with a drop down list i can use to select the collection.
There should be also a search function where a user can enter a word and it would show all written pieces that include the key word.

i would like this to be done using VUE.js, and i will deploy it on my k3s cluster. I want to use vite and tailwind.
i am using mariadb to store the collections and i intend to use minio to store the blobs (text and images).

I have already build a very simple backend with express where i want to build my APIs.
My API is served from harp-api.ekskog.xyz, and at the moment I only have a single API /GET health, that will return OK if the database is up.

I have already scaffolded a vite/vue/tailwind app. I am using javascript.
Create a component that displays the database connection status on the landing page. 
The app should use the inbuilt fetch to interact with the database, not axios or anything like that