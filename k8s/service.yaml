apiVersion: v1
kind: Service
metadata:
  name: harp-api
  namespace: harp
  labels:
    app: harp-api
spec:
  type: LoadBalancer
  selector:
    app: harp-api
  ports:
    - name: http
      port: 80
      targetPort: 3000

