apiVersion: apps/v1
kind: Deployment
metadata:
  name: harp-api
  namespace: webapps
  labels:
    app: harp-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: harp-api
  template:
    metadata:
      labels:
        app: harp-api
    spec:
      volumes:
        - name: harp-nfs
          persistentVolumeClaim:
            claimName: harp-nfs-pvc
      containers:
        - name: harp-api
          image: ghcr.io/ekskog/harp-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: harpo-secret
          volumeMounts:
            - name: harp-nfs
              mountPath: /app/nfs
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: harpo-ui
  namespace: webapps
  labels:
    app: harpo-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: harpo-ui
  template:
    metadata:
      labels:
        app: harpo-ui
    spec:
      containers:
        - name: backend
          image: ghcr.io/ekskog/harpo-ui:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: harpo-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3

